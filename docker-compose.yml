services:
 redis:
  image: redis:7-alpine
  container_name: redis-Testing
  ports:
      - "6379:6379"
  command: ["redis-server","--appendonly","yes"]
  volumes:
      - redis-data:/data
volumes:
  redis-data: